# load library for working with Git
library(usethis)

# edit your gitconfig file from R e.g. to add name and email credentials
# edit_git_config()

# check working directory is correct
getwd()

# set working directory if required
# setwd("<path here>")

# initialize a new git repository from within R
# use_git()

use_git()
